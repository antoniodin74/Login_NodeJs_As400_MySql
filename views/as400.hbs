<h1>AS400</h1>

<div class="row row-cols-1 row-cols-md-4 g-4">
    {{#each as400}}
  <div class="col">
    <div class="card text-bg-light">
      <div class="card-header">
        <h5>{{this.nomeam}}</h5>
      </div>
      <div class="card-body">
        <!--<h5 class="card-title">{{this.nometodo}}</h5>-->
        <p class="card-text limited-text">{{this.descrizioneam}}</p>
        <a href="/as400/primaa" class="btn btn-primary ">Go!</a>
        <!--<a href="/as400/{{this.id}}/edit" class="btn btn-success" title="UPDATE">
          <i class="fa fa-pen-alt"></i>
        </a>
        <form method="post" action="/as400/{{this.id}}" name="form{{this.id}}" style="display: inline">
            <input type="hidden" name="_method" value="DELETE">
            <button type="button" onclick="return showConfirmMsg('Delete \'{{this.nomeam}}\'', 'Sei sicuro di eliminare \'{{this.descrizioneam}}\' ?', 'form{{this.id}}')" class="btn btn-danger" title="DELETE">
                <i class="fa fa-trash-o"></i>
            </button>
        </form>-->
      </div>
       <!-- <div class="card-footer text-muted">
          <p class="card-text"><small class="text-muted">Last updated 3 mins ago</small></p>
        </div>-->
    </div>
  </div>
  {{/each}}
</div>